To install imgui:
  1. Dowload imgui to this directory with `git init .; git remote add origin https://github.com/ocornut/imgui.git; git pull origin master`.
  2. Build and install imgui with `mkdir build; cd build; cmake ..; make install`
